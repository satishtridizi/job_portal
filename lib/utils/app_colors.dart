import 'package:flutter/material.dart';

class AppColors {
  // Background Colors
  static const Color background = Color(0xFF061220);
  static const Color bg = background;

  // Card Colors
  static const Color cardDark = Color(0xFF0D2137);
  static const Color cardMid = Color(0xFF0A1C2E);
  static const Color cardBackground = Color(0xFF0B2534);

  static const Color card = cardDark;

  // Primary Colors
  static const Color primary = Color(0xFFFFCC00);
  static const Color primaryDark = Color(0xFFE6B800);

  // Text Colors
  static const Color textWhite = Color(0xFFFFFFFF);
  static const Color textLight = Color(0xFFE5E7EB);
  static const Color textGrey = Color(0xFF8A9BAE);

  // Border Colors
  static const Color inputBorder = Color(0xFF8C6A00);
  static const Color cardBorder = inputBorder;
  static const Color border = inputBorder;

  // Input Colors
  static const Color inputBackground = Color(0xFF112634);

  // Icon Colors
  static const Color iconBg = Color(0xFF0A1C2E);
  static const Color iconColor = Color(0xFFFFCC00);

  // Badge Colors
  static const Color reviewingBadge = Color(0xFF2A3A4A);
  static const Color shortlistBadge = Color(0xFF0D3040);

  // Button Colors
  static const Color btnDisabled = Color(0xFF1C3347);

  // Chat Screen
  static const Color senderBubble = Color(0xFFFFCC00);
  static const Color receiverBubble = Color(0xFF0A1C2E);

  // ---------- Contact Support Screen ----------
  static const Color quickContactCard = cardDark;
  static const Color commonIssueCard = cardDark;
  static const Color textFieldFill = inputBackground;
  static const Color buttonText = Colors.black;

  // Status Colors
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFFFCC00);
  static const Color error = Color(0xFFEF4444);

  // Divider / Shadow
  static const Color divider = Color(0xFF1E3346);
  static const Color shadow = Color(0x33000000);

  // Transparent Colors
  static const Color transparent = Colors.transparent;
}
